[tool.py-checks]
src = "."

[tool.py-checks.layout.use_cases]
suffix = "UseCase"
required = true

[tool.py-checks.layout.repositories]
suffix = "Repository"
required = true

[tool.py-checks.layout.config]
suffix = "Settings"
required = true
