[project]
name = "drifting"
version = "0"

[tool.python-checks.schema-drift]
# `false` всегда выходит с единицей: правило обязано сказать, что до вердикта
# не дошло, а не промолчать — молчание здесь читалось бы как «схемы сходятся».
alembic = ["false"]
