---
name: Bug report
about: A rule fires where it should not, or stays silent where it should not
title: ''
labels: ''
assignees: ''

---

**Which rule**
The code, e.g. `model-columns`. If the rule is silent and should not be, say
which one you expected.

**The settings it was given**
The section from `py-checks.toml` or `[tool.py-checks]`.

```toml
```

**The smallest file that shows it**

```python
```

**What happened**
The output of `py-checks run` for that file, verbatim.

```
```

**What you expected instead**

**Versions**
- `py-checks --version`:
- `python --version`:
- OS:
