"""Эталонные конфиги, которые библиотека кладёт в проект.

Файлы отсюда `python-checks sync` копирует в `.python-checks/` проекта, а
конфиг проекта их подключает: `extend` у ruff, `extends` у pyright. Копия
лежит в репозитории проекта, потому что ruff и pyright — отдельные программы,
про библиотеку они не знают, а путь внутрь окружения на каждой машине свой.
"""
